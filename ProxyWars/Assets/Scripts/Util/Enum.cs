public enum GameModes {
	FastMode
}

public enum InfluenceModes {
	Absolute,
	Percentage
}
	
public enum Faction {
	Player,
	Enemy,
	Neutral
}

public enum ResourceType {
	Troops,
	Food,
	Gold,
	TradeGoods
}

public enum MissionStatus {
	Locked,
	NoMission,
	CannotDoMissions,
	NotEnoughResources,
	CanCompleteMission
}